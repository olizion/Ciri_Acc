1. Make sure that the override for autoposted postings exists all places where these postings exist. This is places like transactions, balanse, resultatregnskap, årsregnskap etc. When clicking a posting, the user needs to get redirected to the transactions-page and auto open the one that this is, or the bilag page to the dialog - whatever is more correct, where it has to be an override button and the user can choose what they wish to do: Remove it entirely, change the account for the posting etc. Important that this gets reverted in terms of bokføringsloven for this, and that this gets correctly logged for auditing. The underlying functionality for this should already be implemented; frontend is the main missing component but you should verify
2. We need for periodisering a notification alert to the user telling them that they can distribute a given cost and why Ciri finds that a good decision. The bell is already at the top of the application. WE also need the backend logic as well as the frontend functionality when distributing a cost. The user gets the final say for the recommendation from Ciri for what they choose. Ciri will ask for information from the user when the user clicks the notification to select a distribution of cost, in which case they get redirected to a periodisering page with amazing UI where the software nicely renders the timeline it wishes to distrubute over. Ciri will ask for information for the recommended posting if this is correct intuition that the user plans to have this produt over a longer period of time, for any posting that can be distributed according to law.
